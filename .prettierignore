node_modules/
dist/
build/
coverage/
package-lock.json
CHANGELOG.md
# release-please writes the release version into app.json and rewrites the file
# with its own JSON formatter, which expands arrays prettier would keep on one
# line. Formatting it here would fail the release PR's own checks over a file
# nobody hand-formats anyway.
app.json
# Per-machine Claude Code approvals: git-ignored, written by the tool, and full
# of absolute paths nobody should be reformatting.
.claude/settings.local.json
