// The mini app entry point. Everything the game does lives on the watch in
// page/index.js: there is no side service and no phone settings screen, because
// the game needs neither the network nor the phone.
App({
  globalData: {},
  onCreate() {},
  onDestroy() {},
});
